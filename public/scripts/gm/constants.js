/**
 * Shared constants for the GM shell.
 *
 * Mirror of `src/gm-core/campaigns/schemas.js` for values the frontend
 * needs at render time (banner themes for the new-campaign picker, etc.).
 */

export const BANNER_THEMES = ['shadows', 'frontier', 'hollow', 'default'];

export const NARRATOR_ACTOR_ID = 'narrator';
